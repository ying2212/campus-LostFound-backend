# campus-LostFound-backend

### Setup  
Clone the repository:
  ```sh
  git clone https://github.com/ying2212/campus-LostFound-backend.git
  ```

Download the dependencies in package.json:
  ```sh
  npm install
  ```

Run the development server
  ```sh
  npm run dev
  ```
